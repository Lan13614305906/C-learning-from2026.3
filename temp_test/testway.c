#include <stdio.h>

int main() {
    // 定义两个整数
    int a = 10;
    int b = 20;
    int sum = a + b;

    // 输出结果
    printf("a = %d\n", a);
    printf("b = %d\n", b);
    printf("a + b = %d\n", sum);

    return 0;
}